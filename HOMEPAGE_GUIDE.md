# راهنمای صفحه اصلی جدید

## نمای کلی

صفحه اصلی مستندات PMO با طراحی مدرن و الهام‌گرفته از roadmap.sh بازطراحی شده است تا دسترسی به اسناد را آسان‌تر و زیباتر کند.

---

## ویژگی‌های صفحه اصلی

### 🎨 طراحی کارت‌محور
- هر سند به صورت یک کارت زیبا با gradient نمایش داده می‌شود
- آیکون‌های معنادار برای هر سند
- نمایش نسخه در badge زیبا
- انیمیشن hover برای تجربه کاربری بهتر

### 📂 دسته‌بندی منطقی
مستندات در سه دسته اصلی سازماندهی شده‌اند:

#### 1. مدیریت پروژه 🎯
- راهنمای جامع مدیریت پروژه (نسخه 2.0.0-beta)

#### 2. مدیریت ریسک ⚠️
- مرور کلی
- شناسایی ریسک
- ارزیابی ریسک
- پاسخ به ریسک
- کاهش ریسک
- پایش ریسک
- گزارش‌دهی ریسک

#### 3. معماری و استانداردهای فنی 🏗️
- برتری عملیاتی
- امنیت
- قابلیت اطمینان
- بهره‌وری عملکرد
- بهینه‌سازی هزینه
- پایداری
- راهبرد سنجش
- مدیریت داده
- کیفیت کد

---

## تغییرات نسبت به نسخه قبل

### ✅ بهبودها

1. **ساده‌سازی Navigation Bar**
   - حذف منوی طولانی از navbar
   - نگه‌داشتن فقط دکمه "صفحه اصلی"
   - کاهش پیچیدگی UI

2. **بهبود تجربه کاربری**
   - دسترسی سریع‌تر به اسناد
   - نمای بصری بهتر
   - دسته‌بندی واضح‌تر

3. **طراحی مدرن**
   - استفاده از Gradient Cards
   - آیکون‌های emoji برای شناسایی سریع
   - انیمیشن‌های نرم

4. **Responsive Design**
   - نمایش بهینه در موبایل
   - Grid layout انعطاف‌پذیر
   - سازگاری با ابعاد مختلف صفحه

---

## ساختار فایل‌ها

### فایل‌های تغییریافته:

#### 1. `docs/index.md`
```markdown
# صفحه اصلی با:
- معرفی سیستم
- کارت‌های دسته‌بندی شده
- لینک‌های مستقیم به اسناد
```

#### 2. `docs/stylesheets/extra.css`
```css
/* استایل‌های اضافه شده: */
.doc-cards      /* Grid container */
.doc-card       /* کارت تک‌تک اسناد */
.doc-card-icon  /* آیکون سند */
.doc-version    /* Badge نسخه */
```

#### 3. `mkdocs.yml`
```yaml
nav:
  - صفحه اصلی: index.md
# حذف تمام موارد دیگر از nav
```

---

## نحوه استفاده

### برای کاربران
1. باز کردن صفحه اصلی: `http://127.0.0.1:9000`
2. مشاهده دسته‌بندی‌ها
3. کلیک روی کارت مورد نظر
4. مطالعه محتوا

### برای توسعه‌دهندگان

#### افزودن سند جدید:

1. **در فایل `docs/index.md`** کارت جدید اضافه کنید:

```html
<a href="path/to/document/" class="doc-card">
  <div class="doc-card-icon">🔥</div>
  <h3>عنوان سند</h3>
  <p>توضیحات کوتاه</p>
  <span class="doc-version">نسخه X.X.X</span>
</a>
```

2. **سند را در پوشه مناسب قرار دهید**

3. **بیلد کنید:**
```bash
python -m mkdocs build --clean
```

#### تغییر دسته‌بندی:

برای افزودن دسته جدید:

```markdown
### 🆕 نام دسته جدید

<div class="doc-cards">
  <!-- کارت‌های این دسته -->
</div>
```

#### سفارشی‌سازی رنگ‌ها:

در `extra.css` می‌توانید gradient کارت‌ها را تغییر دهید:

```css
.doc-card {
    background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}
```

---

## مزایای طراحی جدید

### 1. برای کاربران نهایی
- ✅ یافتن اسناد سریع‌تر است
- ✅ دسته‌بندی واضح‌تر
- ✅ تجربه بصری بهتر
- ✅ نیازی به جستجو در منو نیست

### 2. برای معماران/مدیران
- ✅ نمای کلی از تمام اسناد در یک صفحه
- ✅ امکان اضافه کردن دسته‌های جدید
- ✅ قابلیت گسترش بالا

### 3. برای توسعه‌دهندگان
- ✅ کد تمیز و قابل نگهداری
- ✅ سفارشی‌سازی آسان
- ✅ استفاده از HTML استاندارد
- ✅ CSS مدولار

---

## نکات فنی

### SEO و دسترسی‌پذیری
- استفاده از تگ‌های معنادار HTML5
- Alt text برای آیکون‌ها (emoji)
- لینک‌های معنادار
- ساختار heading صحیح

### عملکرد
- CSS خالص بدون وابستگی اضافی
- بدون JavaScript اضافی برای کارت‌ها
- بارگذاری سریع
- حجم کم

### سازگاری
- ✅ تمام مرورگرهای مدرن
- ✅ موبایل و تبلت
- ✅ حالت تاریک Material Theme
- ✅ RTL کامل

---

## Troubleshooting

### کارت‌ها به درستی نمایش داده نمی‌شوند

**راه‌حل:**
```bash
# پاک کردن cache مرورگر
Ctrl + Shift + R (در Chrome/Firefox)

# بیلد مجدد
python -m mkdocs build --clean
```

### Gradient نمایش داده نمی‌شود

**بررسی کنید:**
1. فایل `extra.css` در `mkdocs.yml` تعریف شده باشد
2. مرورگر از CSS3 پشتیبانی کند
3. حالت سازگاری (Compatibility Mode) غیرفعال باشد

### لینک‌ها کار نمی‌کنند

**راه‌حل:**
- مسیرها را نسبت به پوشه `docs/` بررسی کنید
- از `/` در ابتدای مسیر استفاده نکنید
- از `/` در انتهای مسیر استفاده کنید

---

## آینده و گسترش

### قابلیت‌های پیشنهادی برای نسخه‌های بعدی

1. **فیلتر کردن:**
   - جستجوی زنده در کارت‌ها
   - فیلتر بر اساس دسته
   - مرتب‌سازی

2. **آمار:**
   - نمایش تعداد بازدید
   - محبوب‌ترین اسناد
   - آخرین به‌روزرسانی‌ها

3. **نشانه‌گذاری:**
   - امکان bookmark کردن
   - لیست اسناد اخیر
   - پیشرفت خواندن

4. **جستجوی پیشرفته:**
   - جستجو در محتوای اسناد
   - پیشنهاد خودکار
   - highlighting نتایج

---

## نتیجه‌گیری

طراحی جدید صفحه اصلی:
- ✅ تجربه کاربری را بهبود می‌دهد
- ✅ دسترسی به اسناد را سریع‌تر می‌کند
- ✅ مقیاس‌پذیری بالایی دارد
- ✅ زیبایی بصری بهتری ارائه می‌دهد

این طراحی با الهام از بهترین practices صنعت (مانند roadmap.sh) و با توجه به نیازهای محلی و فارسی‌زبان پیاده‌سازی شده است.

---

**تاریخ ایجاد:** ۱۴۰۴/۰۸/۱۳  
**نویسنده:** علیرضا بروجردیان  
**نسخه:** 1.0.0
