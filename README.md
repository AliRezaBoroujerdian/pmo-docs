# سیستم مستندسازی PMO

سیستم مستندسازی با پشتیبانی از versioning و export به PDF/Word

## نصب و راه‌اندازی

### پیش‌نیازها

```bash
# نصب dependencies
pip install mkdocs mkdocs-material flask flask-cors beautifulsoup4 markdown
```

### اجرای سرورها

برای استفاده کامل از سیستم، باید دو سرور را به صورت همزمان اجرا کنید:

#### 1. MkDocs Development Server

```bash
mkdocs serve -a 127.0.0.1:9000
```

این سرور مستندات را در آدرس http://127.0.0.1:9000 سرو می‌کند.

#### 2. Export Server

```bash
python export_server.py
```

این سرور API های export را در آدرس http://127.0.0.1:5000 فراهم می‌کند.

## ساختار پروژه

```
pmo-docs/
├── docs/
│   ├── javascripts/
│   │   ├── version-selector.js   # انتخابگر نسخه در sidebar
│   │   └── export-buttons.js     # دکمه‌های export
│   ├── stylesheets/
│   │   ├── fonts/                # فونت‌های Vazirmatn
│   │   └── extra.css             # استایل‌های سفارشی
│   ├── project-management/
│   │   └── versions/
│   │       ├── v1.0.0/
│   │       └── v2.0.0-beta/
│   └── risk-management/
│       └── versions/
│           ├── v1.0.0/
│           ├── v2.0.0-beta/
│           └── v3.0.0/          # نسخه کامل با 7 صفحه
├── export_server.py              # سرور export
├── export_docs.py                # ماژول export (deprecated)
├── exports/                      # فایل‌های export شده
└── mkdocs.yml                    # تنظیمات MkDocs
```

## ویژگی‌ها

### ✅ Version Management
- پشتیبانی از Semantic Versioning (v1.0.0, v2.0.0-beta, v3.0.0)
- Versioning جداگانه برای هر سند
- انتخابگر نسخه هوشمند در sidebar
- مخفی کردن خودکار نسخه‌های دیگر

### ✅ Export Functionality
- دکمه‌های export در پایین sidebar
- Export به PDF با استفاده از Chrome/Edge headless
- Export به Word (نیاز به نصب Pandoc)
- Export تمام صفحات یک نسخه به صورت یکجا
- پشتیبانی از فارسی و RTL در خروجی‌ها

### ✅ Persian/RTL Support
- فونت Vazirmatn (سه وزن: Regular, Medium, Bold)
- پشتیبانی کامل از راست به چپ
- استایل‌های بهینه برای متن فارسی

## استفاده

### مشاهده مستندات

1. سرور MkDocs را اجرا کنید:
   ```bash
   mkdocs serve -a 127.0.0.1:9000
   ```

2. در مرورگر به آدرس http://127.0.0.1:9000 بروید

3. از منوی سمت راست می‌توانید:
   - بین نسخه‌های مختلف جابجا شوید
   - صفحات مختلف را مشاهده کنید

### Export مستندات

1. هر دو سرور را اجرا کنید:
   ```bash
   # Terminal 1
   mkdocs serve -a 127.0.0.1:9000
   
   # Terminal 2
   python export_server.py
   ```

2. در مرورگر، به صفحه مورد نظر بروید

3. در پایین sidebar دکمه‌های export را مشاهده می‌کنید:
   - **📄 دانلود PDF**: Export به فرمت PDF
   - **📝 دانلود Word**: Export به فرمت Word (نیاز به Pandoc)

4. کلیک روی هر کدام، تمام صفحات نسخه فعلی را دانلود می‌کند

### نصب Pandoc (اختیاری - برای Word export)

```bash
choco install pandoc
```

## Export API

Export server یک REST API ساده فراهم می‌کند:

```
GET /export/<doc_type>/<version>/<format>
```

### مثال‌ها:

```bash
# Export risk-management v3.0.0 به PDF
http://127.0.0.1:5000/export/risk-management/v3.0.0/pdf

# Export project-management v1.0.0 به Word
http://127.0.0.1:5000/export/project-management/v1.0.0/word

# Health check
http://127.0.0.1:5000/health
```

## نکات مهم

### Export PDF
- از Chrome یا Edge headless استفاده می‌کند
- فونت فارسی به صورت خودکار اعمال می‌شود
- تمام صفحات یک نسخه در یک فایل PDF ترکیب می‌شوند
- استایل RTL و فارسی حفظ می‌شود

### Export Word
- نیاز به نصب Pandoc دارد
- از فایل‌های Markdown اصلی استفاده می‌کند
- فرمت‌بندی Markdown به Word تبدیل می‌شود

### Cache
- فایل‌های export شده در پوشه `exports/` ذخیره می‌شوند
- Cache هر 30 دقیقه پاک می‌شود
- می‌توانید دستی فایل‌های cache شده را حذف کنید

## توسعه

### اضافه کردن سند جدید

1. ساختار پوشه ایجاد کنید:
   ```
   docs/new-document/versions/v1.0.0/
   ```

2. فایل‌های markdown اضافه کنید

3. در `mkdocs.yml` navigation را به‌روز کنید:
   ```yaml
   nav:
     - New Document:
       - v1.0.0:
         - Page1: new-document/versions/v1.0.0/page1.md
   ```

### اضافه کردن نسخه جدید

1. پوشه نسخه جدید بسازید:
   ```
   docs/document-name/versions/v2.0.0/
   ```

2. فایل‌های markdown را کپی/ویرایش کنید

3. Navigation را در `mkdocs.yml` به‌روز کنید

## مشکلات رایج

### دکمه‌های export کار نمی‌کنند
- مطمئن شوید که export_server.py در حال اجرا است
- Console browser را چک کنید (F12)
- مطمئن شوید که CORS فعال است

### PDF خالی یا بدون فونت فارسی
- مطمئن شوید Chrome یا Edge نصب است
- فونت‌های Vazirmatn در `docs/stylesheets/fonts/` موجودند

### Word export کار نمی‌کند
- Pandoc را نصب کنید: `choco install pandoc`
- مسیر pandoc را به PATH اضافه کنید

## لایسنس

این پروژه برای استفاده داخلی PMO است.
