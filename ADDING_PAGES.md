# 📄 راهنمای اضافه کردن صفحه جدید

این راهنما نحوه اضافه کردن یک صفحه (page) جدید به نسخه موجود یک سند را توضیح می‌دهد.

## 📋 مثال: اضافه کردن صفحه "ابزارهای مدیریت ریسک"

فرض کنید می‌خواهیم صفحه "ابزارهای مدیریت ریسک" را به v3.0.0 اضافه کنیم.

---

## 🎯 انواع صفحات

### 1. صفحه محتوایی ساده

فایل Markdown با متن، لیست، جدول و تصاویر.

```markdown
# عنوان صفحه

محتوای اصلی...
```

### 2. صفحه مرجع/API

مستندات API یا کدهای نمونه.

```markdown
# API Reference

## Function: calculateRisk()

**سینتکس:**
```python
risk_score = calculateRisk(probability, impact)
```

**پارامترها:**
- `probability`: احتمال (0-1)
- `impact`: تأثیر (0-1)

**خروجی:** امتیاز ریسک (0-1)
```

### 3. صفحه Tutorial

راهنمای گام به گام.

```markdown
# آموزش: ایجاد ماتریس ریسک

## مرحله 1: تعریف معیارها
...

## مرحله 2: جمع‌آوری داده
...
```

---

## 🔧 مرحله 1: ایجاد فایل صفحه

### 1.1 ایجاد فایل Markdown

```powershell
New-Item -Path "docs/risk-management/versions/v3.0.0/tools.md" -ItemType File
```

### 1.2 نام‌گذاری استاندارد

**قواعد نام‌گذاری:**
- همیشه lowercase
- استفاده از dash برای جداسازی کلمات
- بدون فاصله یا underline
- پسوند `.md`

**مثال‌های صحیح:**
- `tools.md`
- `risk-tools.md`
- `assessment-methods.md`
- `case-studies.md`

**مثال‌های نادرست:**
- ~~`Tools.md`~~ (uppercase)
- ~~`risk_tools.md`~~ (underscore)
- ~~`risk tools.md`~~ (space)
- ~~`tools`~~ (بدون پسوند)

---

## 📝 مرحله 2: نوشتن محتوا

### 2.1 ساختار پایه

```markdown
# ابزارهای مدیریت ریسک

> این صفحه ابزارهای مختلف برای مدیریت ریسک را معرفی می‌کند.

## نمای کلی

مدیریت ریسک نیاز به ابزارهای مختلفی دارد که در این بخش به آن‌ها می‌پردازیم.

## دسته‌بندی ابزارها

### 1. ابزارهای شناسایی

#### 1.1 تکنیک‌های Brainstorming
...

#### 1.2 تحلیل SWOT
...

### 2. ابزارهای ارزیابی

#### 2.1 ماتریس احتمال-تأثیر
...

#### 2.2 تحلیل Monte Carlo
...

## مثال‌های کاربردی

### مثال 1: استفاده از SWOT

...

## منابع و مراجع

- [ISO 31000](https://www.iso.org/)
- [PMBOK Guide](https://www.pmi.org/)

---

**تاریخ بروزرسانی:** ۱۱ آبان ۱۴۰۴
```

### 2.2 عناصر مهم Markdown

#### Headers (سرفصل‌ها)

```markdown
# H1 - عنوان اصلی صفحه (فقط یک بار)
## H2 - بخش اصلی
### H3 - زیربخش
#### H4 - جزئیات
```

#### Lists (لیست‌ها)

```markdown
## لیست شماره‌دار
1. مورد اول
2. مورد دوم
3. مورد سوم

## لیست نقطه‌ای
- مورد اول
- مورد دوم
  - زیرمورد
  - زیرمورد
- مورد سوم
```

#### Tables (جداول)

```markdown
| ابزار | نوع | سطح پیچیدگی |
|-------|-----|-------------|
| SWOT | تحلیلی | پایین |
| Monte Carlo | شبیه‌سازی | بالا |
| Risk Matrix | بصری | متوسط |
```

#### Code Blocks (کد)

````markdown
```python
def calculate_risk_score(probability, impact):
    """
    محاسبه امتیاز ریسک
    """
    return probability * impact
```
````

#### Admonitions (هشدارها)

```markdown
!!! note "نکته"
    این یک نکته مهم است.

!!! warning "هشدار"
    مراقب این مورد باشید.

!!! tip "نکته کاربردی"
    برای بهترین نتیجه...
```

#### Links (لینک‌ها)

```markdown
<!-- لینک خارجی -->
[ISO 31000](https://www.iso.org/)

<!-- لینک داخلی به صفحه دیگر همین نسخه -->
[نمای کلی](overview.md)

<!-- لینک به نسخه دیگر -->
[نسخه 2.0.0](../../v2.0.0/overview.md)
```

#### Images (تصاویر)

```markdown
<!-- تصویر محلی -->
![Risk Matrix](../../../assets/images/risk-matrix.png)

<!-- تصویر خارجی -->
![Example](https://example.com/image.png)
```

---

## ⚙️ مرحله 3: بروزرسانی mkdocs.yml

باز کردن `mkdocs.yml` و اضافه کردن صفحه جدید:

```yaml
nav:
  - خانه: index.md
  
  - مدیریت ریسک:
    - v3.0.0:
      - نمای کلی: risk-management/versions/v3.0.0/overview.md
      - شناسایی: risk-management/versions/v3.0.0/identification.md
      - ارزیابی: risk-management/versions/v3.0.0/assessment.md
      - کاهش: risk-management/versions/v3.0.0/mitigation.md
      - پاسخ: risk-management/versions/v3.0.0/response.md
      - نظارت: risk-management/versions/v3.0.0/monitoring.md
      - گزارش‌دهی: risk-management/versions/v3.0.0/reporting.md
      - ابزارها: risk-management/versions/v3.0.0/tools.md  # ⬅️ جدید
```

**نکته:** ترتیب صفحات در navigation مهم است - طبق منطق محتوا مرتب کنید.

---

## 📊 مرحله 4: بروزرسانی export_server.py (اختیاری)

اگر می‌خواهید صفحه جدید در export شامل شود:

```python
PAGE_MAPPINGS = {
    'risk-management': {
        'v3.0.0': [
            'overview',
            'identification',
            'assessment',
            'mitigation',
            'response',
            'monitoring',
            'reporting',
            'tools'  # ⬅️ جدید (بدون .md)
        ],
        # ... بقیه نسخه‌ها
    },
    # ...
}
```

---

## 🏗️ مرحله 5: Build و Test

### 5.1 Build

```powershell
mkdocs build --clean
```

بررسی warnings:
```
INFO - Building documentation...
INFO - Cleaning site directory
INFO - Documentation built in 0.XX seconds
```

### 5.2 Test محلی

```powershell
mkdocs serve -a 127.0.0.1:9000
```

### 5.3 چک‌لیست تست

- [ ] صفحه جدید در sidebar navigation نمایش داده می‌شود
- [ ] کلیک روی صفحه → باز می‌شود
- [ ] فونت فارسی صحیح نمایش داده می‌شود
- [ ] جداول درست render شده‌اند
- [ ] Code blocks با syntax highlighting نمایش داده می‌شوند
- [ ] تصاویر (اگر هست) نمایش داده می‌شوند
- [ ] لینک‌های داخلی کار می‌کنند
- [ ] لینک‌های خارجی کار می‌کنند
- [ ] Version selector در این صفحه کار می‌کند
- [ ] Export شامل صفحه جدید می‌شود

---

## 🔗 مرحله 6: ایجاد لینک‌های متقاطع

### 6.1 لینک از صفحات دیگر

در `overview.md`:

```markdown
## بخش‌های این سند

این مستندات شامل موارد زیر است:

- [شناسایی ریسک](identification.md)
- [ارزیابی ریسک](assessment.md)
- [کاهش ریسک](mitigation.md)
- [پاسخ به ریسک](response.md)
- [نظارت بر ریسک](monitoring.md)
- [گزارش‌دهی](reporting.md)
- **[ابزارهای مدیریت ریسک](tools.md)** ← جدید
```

### 6.2 لینک در صفحه جدید

در `tools.md`:

```markdown
## مطالعه بیشتر

برای اطلاعات تکمیلی، صفحات زیر را ببینید:

- [ارزیابی ریسک](assessment.md) - روش‌های ارزیابی
- [کاهش ریسک](mitigation.md) - استراتژی‌های کاهش
```

---

## 📊 مرحله 7: اضافه کردن محتوای تکمیلی

### 7.1 اضافه کردن تصویر

1. قرار دادن فایل تصویر:
```powershell
New-Item -Path "docs/assets/images" -ItemType Directory -Force
# کپی تصویر به docs/assets/images/risk-tools.png
```

2. استفاده در markdown:
```markdown
![ابزارهای ریسک](../../../assets/images/risk-tools.png)
```

### 7.2 اضافه کردن کد نمونه

```markdown
## مثال Python: محاسبه Risk Score

```python
class RiskCalculator:
    """
    کلاس محاسبه امتیاز ریسک
    """
    
    def __init__(self, probability, impact):
        self.probability = probability
        self.impact = impact
    
    def calculate_score(self):
        """
        محاسبه امتیاز با فرمول P × I
        """
        return self.probability * self.impact
    
    def get_severity_level(self):
        """
        تعیین سطح شدت
        """
        score = self.calculate_score()
        if score >= 0.7:
            return "بحرانی"
        elif score >= 0.4:
            return "بالا"
        elif score >= 0.2:
            return "متوسط"
        else:
            return "پایین"

# استفاده
risk = RiskCalculator(probability=0.7, impact=0.9)
print(f"امتیاز ریسک: {risk.calculate_score()}")
print(f"سطح: {risk.get_severity_level()}")
```

**خروجی:**
```
امتیاز ریسک: 0.63
سطح: بالا
```
```

### 7.3 اضافه کردن جدول پیشرفته

```markdown
## مقایسه ابزارها

| ابزار | نوع | زمان اجرا | دقت | هزینه | پیچیدگی |
|-------|-----|----------|------|-------|---------|
| SWOT Analysis | کیفی | 1-2 ساعت | متوسط | کم | پایین |
| Risk Matrix | کمی | 30 دقیقه | متوسط | کم | پایین |
| Monte Carlo | کمی | 1-3 روز | بالا | زیاد | بالا |
| Fault Tree Analysis | کمی | 2-5 روز | خیلی بالا | زیاد | بالا |
| Delphi Technique | کیفی | 1-2 هفته | بالا | متوسط | متوسط |

### راهنمای انتخاب

- **پروژه‌های کوچک:** SWOT یا Risk Matrix
- **پروژه‌های متوسط:** Delphi Technique
- **پروژه‌های بزرگ:** Monte Carlo یا FTA
```

---

## ✅ مرحله 8: Commit به Git

```powershell
git add docs/risk-management/versions/v3.0.0/tools.md
git add mkdocs.yml
git add export_server.py

git commit -m "Add Risk Management Tools page to v3.0.0

New page: tools.md

Content:
- Overview of risk management tools
- Categorization (Identification, Assessment, Mitigation)
- Tool comparison table
- Python code examples
- Practical case studies

Updates:
- mkdocs.yml: added tools.md to navigation
- export_server.py: added 'tools' to v3.0.0 pages"

git push origin main
```

---

## 📝 چک‌لیست نهایی

قبل از commit:

- [ ] فایل .md با نام‌گذاری صحیح ایجاد شده
- [ ] محتوا کامل و بدون خطای تایپی است
- [ ] Headers به ترتیب صحیح استفاده شده (H1 → H2 → H3)
- [ ] جداول و کدها صحیح render می‌شوند
- [ ] تصاویر (اگر هست) نمایش داده می‌شوند
- [ ] لینک‌های داخلی صحیح هستند
- [ ] لینک‌های خارجی کار می‌کنند
- [ ] mkdocs.yml بروزرسانی شده
- [ ] export_server.py بروزرسانی شده (اگر نیاز بود)
- [ ] لینک‌های متقاطع در صفحات دیگر اضافه شده
- [ ] Build بدون error
- [ ] تست محلی کامل
- [ ] Navigation کار می‌کند
- [ ] Export شامل صفحه جدید می‌شود

---

## 🎯 نکات مهم

### Best Practices

1. **یک صفحه، یک موضوع**
   - هر صفحه باید یک موضوع مشخص پوشش دهد
   - صفحات خیلی طولانی را تقسیم کنید

2. **ساختار واضح**
   - از headers به ترتیب استفاده کنید
   - بخش‌بندی منطقی داشته باشید

3. **مثال‌های کاربردی**
   - کدهای نمونه working باشند
   - مثال‌های واقعی بزنید

4. **لینک‌دهی مناسب**
   - به صفحات مرتبط لینک بدهید
   - از broken links جلوگیری کنید

5. **بروزرسانی منظم**
   - تاریخ آخرین بروزرسانی را اضافه کنید
   - محتوای قدیمی را به‌روز کنید

### مشکلات رایج

#### مشکل: صفحه در navigation نمایش داده نمی‌شود

**راه‌حل:** بررسی `mkdocs.yml` - مطمئن شوید indent صحیح است.

#### مشکل: تصویر نمایش داده نمی‌شود

**راه‌حل:** بررسی مسیر نسبی:
```markdown
<!-- از docs/risk-management/versions/v3.0.0/tools.md -->
<!-- به docs/assets/images/risk-tools.png -->
![Risk Tools](../../../assets/images/risk-tools.png)
```

#### مشکل: کد فارسی در code block خراب است

**راه‌حل:** از کامنت فارسی در کد استفاده کنید:

```python
# محاسبه ریسک
def calculate_risk():
    pass
```

#### مشکل: جدول راست‌چین نمی‌شود

**راه‌حل:** در `extra.css` alignment جداول تنظیم شده است - بدون نیاز به تغییر.

---

## 🚀 مراحل بعدی

بعد از اضافه کردن صفحه:

1. [اضافه کردن نسخه جدید](ADDING_VERSIONS.md) - برای انتشار نسخه جدید
2. [اضافه کردن سند جدید](ADDING_DOCUMENTS.md) - برای سند کاملاً جدید

---

## ❓ سوالات متداول

### چند صفحه می‌توان به یک نسخه اضافه کرد؟

نامحدود! اما برای خوانایی، توصیه می‌شود زیر 15-20 صفحه باشد.

### آیا می‌توان صفحه را بعداً حذف کرد؟

بله، اما بهتر است صفحه را **deprecated** کنید به جای حذف:

```markdown
# ابزارها (Deprecated)

> ⚠️ **هشدار:** این صفحه deprecated شده است.  
> لطفاً به [ابزارهای جدید](new-tools.md) مراجعه کنید.
```

### چگونه ترتیب صفحات را تغییر دهم؟

در `mkdocs.yml` ترتیب را تغییر دهید:

```yaml
- v3.0.0:
  - نمای کلی: ...
  - ابزارها: ...      # ⬅️ جابجا شد
  - شناسایی: ...
```

### آیا باید صفحه را به تمام نسخه‌ها اضافه کنم؟

خیر، فقط به نسخه‌ای که می‌خواهید اضافه کنید. نسخه‌های قدیمی‌تر نیازی به تغییر ندارند.

---

**تاریخ آخرین بروزرسانی:** ۱۱ آبان ۱۴۰۴
