# راهنمای استفاده از سیستم Export

## ✅ تغییرات انجام شده

### مشکل اولیه
- **PDF Export**: فایل دانلود می‌شد اما بعد از دانلود خطا نشان می‌داد
- **Word Export**: اصلاً کار نمی‌کرد (Pandoc نیاز به admin access داشت)

### راه‌حل‌ها

#### 1. PDF Export - رفع خطای بعد از دانلود
- **مشکل**: استفاده از `fetch()` API باعث ایجاد خطا در handling blob می‌شد
- **راه‌حل**: 
  - تغییر به `XMLHttpRequest` برای کنترل بهتر download
  - اضافه کردن `setTimeout` برای cleanup بعد از download
  - بهبود error handling

#### 2. Word Export - پیاده‌سازی بدون Pandoc
- **مشکل**: Pandoc نیاز به admin access برای نصب داشت
- **راه‌حل**:
  - استفاده از کتابخانه `python-docx` به جای Pandoc
  - پیاده‌سازی parser سفارشی برای Markdown به Word
  - پشتیبانی از RTL و فرمت‌بندی فارسی

## 🚀 نحوه استفاده

### قدم 1: اطمینان از اجرای سرورها

باید **دو سرور** به صورت همزمان در حال اجرا باشند:

#### Terminal 1 - MkDocs Server
```powershell
mkdocs serve -a 127.0.0.1:9000
```
📍 **آدرس**: <http://127.0.0.1:9000>

#### Terminal 2 - Export Server
```powershell
python export_server.py
# یا با مسیر کامل:
C:\Users\ALIREZA\AppData\Local\Programs\Python\Python313\python.exe export_server.py
```
📍 **آدرس**: <http://127.0.0.1:5000>

### قدم 2: مشاهده مستندات

1. مرورگر را باز کنید
2. به آدرس <http://127.0.0.1:9000> بروید
3. از منوی سمت راست (sidebar):
   - سند مورد نظر را انتخاب کنید (مثلاً Risk Management)
   - نسخه مورد نظر را انتخاب کنید (مثلاً v3.0.0)

### قدم 3: Export مستندات

در پایین sidebar (زیر لیست نسخه‌ها) دو دکمه می‌بینید:

#### 📄 دانلود PDF
- **کلیک کنید** → فایل PDF تمام صفحات نسخه فعلی دانلود می‌شود
- **نام فایل**: `<سند>-<نسخه>.pdf` (مثلاً: `risk-management-v3.0.0.pdf`)
- **محتوا**: تمام 7 صفحه risk-management نسخه v3.0.0 در یک PDF
- **ویژگی‌ها**:
  - فونت فارسی (Vazirmatn)
  - استایل RTL
  - هدر با نام سند و نسخه
  - فوتر با اطلاعات تولید

#### 📝 دانلود Word
- **کلیک کنید** → فایل Word (.docx) تمام صفحات نسخه فعلی دانلود می‌شود
- **نام فایل**: `<سند>-<نسخه>.docx` (مثلاً: `risk-management-v3.0.0.docx`)
- **محتوا**: تمام صفحات markdown به Word تبدیل می‌شوند
- **ویژگی‌ها**:
  - فرمت‌بندی RTL
  - Headers با استایل‌های مختلف (H1, H2, H3)
  - Bullet points و numbered lists
  - رنگ‌بندی headers

### قدم 4: پیدا کردن فایل‌های دانلود شده

فایل‌ها در دو جا ذخیره می‌شوند:

1. **پوشه دانلود مرورگر** (معمولاً `Downloads`)
2. **پوشه exports در پروژه**: `D:\PMO\Doc\pmo-docs\exports\`

## 🔧 عیب‌یابی

### دکمه‌های export نمایش داده نمی‌شوند
1. مطمئن شوید که `mkdocs serve` در حال اجرا است
2. صفحه را Refresh کنید (F5 یا Ctrl+F5)
3. Console browser را چک کنید (F12 → Console)

### PDF دانلود نمی‌شود
1. مطمئن شوید `export_server.py` در حال اجرا است
2. Chrome یا Edge را باز کنید و به <http://127.0.0.1:5000/health> بروید
3. باید پیام `{"status": "ok"}` را ببینید
4. Console browser را چک کنید برای خطاهای CORS

### Word دانلود نمی‌شود
1. مطمئن شوید `python-docx` نصب است:
   ```powershell
   pip install python-docx
   ```
2. Terminal export server را چک کنید برای خطاها
3. اگر خطای encoding دیدید، مطمئن شوید فایل‌های markdown با UTF-8 ذخیره شده‌اند

### خطا بعد از دانلود PDF
✅ **حل شد!** این مشکل با تغییر به XMLHttpRequest برطرف شده است.

اگر هنوز خطا می‌بینید:
1. Cache browser را پاک کنید (Ctrl+Shift+Delete)
2. صفحه را Hard Refresh کنید (Ctrl+F5)

### سرور export متوقف می‌شود
1. خطاهای Python را در terminal بررسی کنید
2. مطمئن شوید port 5000 آزاد است:
   ```powershell
   netstat -ano | findstr :5000
   ```
3. اگر port در استفاده است، process را kill کنید:
   ```powershell
   taskkill /F /PID <process_id>
   ```

## 📊 ویژگی‌های Export

### PDF Export
- ✅ تمام صفحات یک نسخه در یک فایل
- ✅ فونت فارسی (Vazirmatn fallback به Tahoma)
- ✅ استایل RTL
- ✅ رنگ‌بندی headers
- ✅ Table of contents (headers)
- ✅ Code blocks با background
- ✅ Blockquotes با استایل
- ✅ Cache برای performance

### Word Export
- ✅ تمام صفحات markdown در یک فایل
- ✅ تبدیل headers (H1, H2, H3)
- ✅ Bullet lists و numbered lists
- ✅ Alignment راست‌چین (RTL)
- ✅ رنگ‌بندی headers
- ✅ Font sizes مناسب
- ✅ Page breaks بین فایل‌ها

## 🎯 نکات مهم

### Cache
- فایل‌های export شده cache می‌شوند
- هر 30 دقیقه cache پاک می‌شود
- برای پاک کردن دستی cache:
  ```powershell
  Remove-Item exports\* -Force
  ```

### Performance
- اولین export هر نسخه ~5-10 ثانیه طول می‌کشد
- export های بعدی از cache استفاده می‌کنند (instant)
- PDF generation با Chrome/Edge headless

### فرمت فایل‌ها
- PDF: A4, margins 2cm, RTL
- Word: .docx (Office 2007+), RTL alignment

## 📝 مثال‌های Export

### Risk Management v3.0.0
- **7 صفحه**: overview, identification, assessment, response, mitigation, monitoring, reporting
- **PDF حجم**: ~500KB
- **Word حجم**: ~50KB
- **زمان تولید**: ~8 ثانیه (اولین بار)

### Project Management v1.0.0
- **1 صفحه**: guide
- **PDF حجم**: ~200KB
- **Word حجم**: ~20KB
- **زمان تولید**: ~3 ثانیه (اولین بار)

## 🚦 وضعیت فعلی

### ✅ کار می‌کند
- PDF export برای تمام نسخه‌ها
- Word export برای تمام نسخه‌ها
- Version selector در sidebar
- Navigation بین نسخه‌ها
- Caching و performance
- RTL و فونت فارسی

### 🔄 در حال توسعه
- بهبود فرمت Word (tables, images)
- اضافه کردن metadata به PDF
- گزینه انتخاب صفحات خاص برای export
- Export history و logs

### 📋 برنامه آینده
- Export به HTML (تک فایل)
- Export به Markdown (combined)
- Scheduling برای export خودکار
- Email notification بعد از export

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. Log های terminal را بررسی کنید
2. Browser console را چک کنید (F12)
3. مطمئن شوید هر دو سرور در حال اجراند
4. Cache را پاک کنید

**تاریخ به‌روزرسانی**: 1 نوامبر 2025
