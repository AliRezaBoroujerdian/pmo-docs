# ❓ راهنمای استفاده از سیستم

این راهنما نحوه استفاده از سیستم مستندسازی PMO را برای کاربران نهایی توضیح می‌دهد.

## 🚀 شروع سریع

### پیش‌نیاز: اجرای سرورها

برای استفاده از سیستم، باید **دو سرور** به صورت همزمان اجرا شوند:

#### روش 1: استفاده از Batch File (آسان)

```powershell
.\start-servers.bat
```

این فایل:
- سرور MkDocs را روی پورت 9000 اجرا می‌کند
- سرور Export را روی پورت 5000 اجرا می‌کند  
- خودکار مرورگر را باز می‌کند

#### روش 2: اجرای دستی

**Terminal 1 - MkDocs Server:**
```powershell
mkdocs serve -a 127.0.0.1:9000
```

**Terminal 2 - Export Server:**
```powershell
python export_server.py
```

---

## 📚 مشاهده مستندات

### 1. باز کردن مرورگر

به آدرس زیر بروید:
```
http://127.0.0.1:9000
```

### 2. انتخاب سند

در navigation (منوی سمت راست)، سند مورد نظر را انتخاب کنید:
- **مدیریت پروژه**
- **مدیریت ریسک**

### 3. انتقال خودکار به آخرین نسخه

وقتی روی یک سند کلیک می‌کنید، **خودکار به آخرین نسخه** آن سند منتقل می‌شوید:
- مدیریت ریسک → v3.0.0
- مدیریت پروژه → v2.0.0-beta

### 4. تغییر نسخه

اگر می‌خواهید نسخه دیگری را ببینید:
1. در **sidebar** (منوی سمت راست)
2. بخش **"انتخاب نسخه"** را پیدا کنید
3. از dropdown نسخه مورد نظر را انتخاب کنید
4. صفحه خودکار به نسخه انتخابی تغییر می‌کند

---

## 📥 خروجی گرفتن (Export)

### دکمه‌های Export

در پایین sidebar، دو دکمه export می‌بینید:
- **📄 دانلود PDF**
- **📝 دانلود Word**

### Export به PDF

1. روی دکمه **"📄 دانلود PDF"** کلیک کنید
2. فایل خودکار دانلود می‌شود
3. فایل شامل **تمام صفحات** نسخه فعلی است

**ویژگی‌های PDF:**
- فونت فارسی (Vazirmatn)
- استایل راست‌چین (RTL)
- رنگ‌بندی headers
- Code blocks با syntax highlighting
- Blockquotes و tables
- هدر با نام سند و نسخه

**نام فایل:**
```
<سند>_<نسخه>_<تاریخ>_<ساعت>.pdf
```

مثال:
```
risk-management_v3.0.0_2024-11-01_14-30-45.pdf
```

### Export به Word

1. روی دکمه **"📝 دانلود Word"** کلیک کنید
2. فایل خودکار دانلود می‌شود
3. فایل شامل **تمام صفحات** نسخه فعلی است

**ویژگی‌های Word:**
- فرمت DOCX (Office 2007+)
- فرمت‌بندی راست‌چین (RTL)
- Headers با استایل‌های مختلف
- Bullet points و numbered lists
- رنگ‌بندی headers

**نام فایل:**
```
<سند>_<نسخه>_<تاریخ>_<ساعت>.docx
```

### محل ذخیره فایل‌ها

فایل‌های export شده در دو جا قرار می‌گیرند:

1. **پوشه دانلود مرورگر** (معمولاً `Downloads`)
2. **پوشه exports در پروژه:**
   ```
   D:\PMO\Doc\pmo-docs\exports\
   ```

---

## 🔍 جستجو در مستندات

### استفاده از جستجو

1. در بالای صفحه، کادر جستجو را می‌بینید
2. کلمه کلیدی را تایپ کنید
3. نتایج به صورت لحظه‌ای نمایش داده می‌شوند
4. روی نتیجه کلیک کنید تا به آن صفحه بروید

**جستجو شامل:**
- تمام محتوای متنی
- Headers و عناوین
- تمام نسخه‌ها
- تمام اسناد

---

## 🎨 ویژگی‌های رابط کاربری

### Navigation (منوی راست)

شامل:
- لیست اسناد (مدیریت پروژه، مدیریت ریسک)
- لیست نسخه‌ها
- لیست صفحات هر نسخه
- انتخابگر نسخه (Version Selector)
- دکمه‌های Export

### محتوای صفحه

شامل:
- متن با فونت فارسی
- راست‌چین (RTL)
- جداول
- Code blocks
- لیست‌ها
- تصاویر
- Blockquotes

### Table of Contents

در سمت چپ (یا بالا در موبایل):
- لیست headers صفحه فعلی
- کلیک برای پرش به بخش مورد نظر

---

## 📱 استفاده در موبایل

سیستم **Responsive** است و در موبایل و تبلت کار می‌کند:

### در موبایل:

1. همبرگر منو (☰) را بزنید
2. Navigation باز می‌شود
3. سند و نسخه را انتخاب کنید
4. Export هم کار می‌کند

---

## 💡 نکات کاربردی

### نکته 1: Keyboard Shortcuts

- **`/`** یا **`S`**: باز کردن جستجو
- **`←` / `→`**: رفتن به صفحه قبل/بعد
- **`ESC`**: بستن جستجو

### نکته 2: Cache Export

- اولین export هر نسخه ~5-10 ثانیه طول می‌کشد
- Export های بعدی از cache استفاده می‌کنند (فوری)
- Cache هر 30 دقیقه پاک می‌شود

### نکته 3: لینک‌های مستقیم

می‌توانید URL صفحات را bookmark کنید:
```
http://127.0.0.1:9000/risk-management/versions/v3.0.0/overview/
```

### نکته 4: چند نسخه همزمان

می‌توانید چند tab باز کنید و نسخه‌های مختلف را مقایسه کنید.

---

## 🔧 مشکلات رایج

### مشکل 1: صفحه باز نمی‌شود

**راه‌حل:**
1. مطمئن شوید سرور MkDocs در حال اجرا است
2. آدرس را چک کنید: `http://127.0.0.1:9000`
3. Terminal سرور را چک کنید برای خطا

### مشکل 2: دکمه‌های Export نمایش داده نمی‌شوند

**راه‌حل:**
1. مطمئن شوید سرور Export در حال اجرا است
2. صفحه را Refresh کنید (F5)
3. Browser Console را چک کنید (F12 → Console)

### مشکل 3: Export کار نمی‌کند

**راه‌حل:**
1. مطمئن شوید هر دو سرور در حال اجراند
2. به `http://127.0.0.1:5000/health` بروید
3. باید `{"status": "ok"}` را ببینید
4. اگر خطا بود، Terminal export server را چک کنید

### مشکل 4: فونت فارسی نمایش داده نمی‌شود

**راه‌حل:**
1. Cache مرورگر را پاک کنید (Ctrl+Shift+Delete)
2. Hard Refresh کنید (Ctrl+Shift+R)
3. مطمئن شوید فونت‌ها در `docs/stylesheets/fonts/` وجود دارند

### مشکل 5: Auto-redirect کار نمی‌کند

**راه‌حل:**
1. Cache مرورگر را پاک کنید
2. Hard Refresh کنید (Ctrl+Shift+R)
3. JavaScript Console را چک کنید (F12)

---

## 📊 مثال‌های Export

### مدیریت ریسک v3.0.0

**محتوا:**
- 7 صفحه: overview, identification, assessment, mitigation, response, monitoring, reporting

**فایل PDF:**
- حجم: ~500 KB
- صفحات: ~25 صفحه A4
- زمان تولید: ~8 ثانیه (اولین بار)

**فایل Word:**
- حجم: ~50 KB
- صفحات: ~20 صفحه
- زمان تولید: ~3 ثانیه (اولین بار)

### مدیریت پروژه v2.0.0-beta

**محتوا:**
- 1 صفحه: guide

**فایل PDF:**
- حجم: ~200 KB
- صفحات: ~10 صفحه A4
- زمان تولید: ~3 ثانیه

**فایل Word:**
- حجم: ~20 KB
- صفحات: ~8 صفحه
- زمان تولید: ~1 ثانیه

---

## ❓ سوالات متداول

### چند سند موجود است؟

در حال حاضر 2 سند:
- مدیریت پروژه (2 نسخه)
- مدیریت ریسک (3 نسخه)

### آیا می‌توانم فقط یک صفحه export کنم؟

در حال حاضر خیر. Export تمام صفحات یک نسخه را شامل می‌شود.

### آیا می‌توانم export را شخصی‌سازی کنم?

بله، با ویرایش `export_server.py` می‌توانید:
- تغییر استایل PDF
- تغییر فرمت Word
- اضافه کردن watermark
- تغییر header/footer

### چطور می‌توانم سند جدید اضافه کنم؟

راهنمای [ADDING_DOCUMENTS.md](ADDING_DOCUMENTS.md) را مطالعه کنید.

### چطور می‌توانم نسخه جدید منتشر کنم؟

راهنمای [ADDING_VERSIONS.md](ADDING_VERSIONS.md) را مطالعه کنید.

---

## 📞 پشتیبانی

اگر مشکل دارید:

1. [TROUBLESHOOTING.md](TROUBLESHOOTING.md) را مطالعه کنید
2. Terminal logs را بررسی کنید
3. Browser Console را چک کنید (F12)
4. Issue جدید در repository ایجاد کنید

---

## 🎓 یادگیری بیشتر

برای یادگیری عمیق‌تر:

- [📚 راهنمای مستندات](DOCUMENTATION_GUIDE.md) - کدام راهنما را بخوانم؟
- [📁 ساختار پروژه](PROJECT_STRUCTURE.md) - ساختار کامل
- [📥 راهنمای نصب](INSTALLATION.md) - نصب محیط خودتان

---

**تاریخ بروزرسانی:** ۱۱ آبان ۱۴۰۴ (November 1, 2025)
